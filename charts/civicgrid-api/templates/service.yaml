apiVersion: v1
kind: Service
metadata:
  name: {{ include "civicgrid-api.fullname" . }}
  labels:
    {{- include "civicgrid-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "civicgrid-api.selectorLabels" . | nindent 4 }}
